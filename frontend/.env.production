# Environment variables for PRODUCTION build (npm run build)

# Assumes backend API is served relative to the frontend host (e.g., via nginx proxy in deployment)
VITE_API_URL=/api

# Do NOT use mock data in production builds
VITE_USE_MOCK_DATA=false
