# Environment variables for DEVELOPMENT mode (npm run dev)

# Points to your locally running backend server
VITE_API_URL=http://localhost:8000/api

# Use mock data during development by default? Set to false to hit local backend.
VITE_USE_MOCK_DATA=true
